# Qwen Training Pipeline

source: qwen_docs
topic: training
tags: pre-training, post-training, SFT, RL, GRPO, DPO, pipeline

## Pre-training (Qwen3)

Dataset: ~36 trillion tokens across 119 languages (2× Qwen2.5's 18T).

Data sources:
- Web crawl (multilingual)
- PDF documents extracted with Qwen2.5-VL
- Synthetic math data (Qwen2.5-Math)
- Synthetic code data (Qwen2.5-Coder)
- Textbooks, Q&A pairs, code snippets

### Three-stage pre-training:

**Stage 1 (S1)**: 30T+ tokens, 4K context. Basic language skills, general knowledge.

**Stage 2 (S2)**: +5T tokens. Increased STEM, coding, reasoning data proportion.

**Stage 3**: High-quality long-context data. Context extended to 32K tokens.

**Result**: Qwen3 dense base models match Qwen2.5 models with 2× more parameters.
- Qwen3-4B ≈ Qwen2.5-7B performance
- Qwen3-8B ≈ Qwen2.5-14B performance
- MoE models achieve Qwen2.5 dense performance with only 10% of active parameters

## Post-training Pipeline (4 Stages)

### Stage 1: Long CoT Cold Start (SFT)
Fine-tune on diverse long chain-of-thought (CoT) data covering:
- Mathematics
- Coding
- Logical reasoning
- STEM problems

Goal: Equip model with foundational reasoning abilities.

### Stage 2: Reasoning RL
Scale up compute for reinforcement learning with rule-based rewards.
- Enhances exploration and exploitation
- Strengthens step-by-step reasoning

### Stage 3: Thinking Mode Fusion (SFT)
Integrate non-thinking capabilities into the thinking model.
- Fine-tune on 75% long CoT data + 25% standard instruction-tuning data
- Data generated by Stage 2 enhanced thinking model
- Creates hybrid model supporting both modes seamlessly

Recommended dataset ratio for fine-tuning to preserve reasoning:
**75% reasoning : 25% non-reasoning** (e.g., NVIDIA math-reasoning + Maxime's FineTome)

### Stage 4: General RL
Apply RL across 20+ general-domain tasks:
- Instruction following
- Format following
- Agent capabilities
- Corrects undesired behaviors

## Training Frameworks Supported

| Framework | Notes |
|---|---|
| Unsloth | 2× faster, 70% less VRAM, LoRA/QLoRA/full, RLHF |
| LLaMA-Factory | Full fine-tuning, LoRA, multimodal |
| Axolotl | YAML-config driven SFT/DPO |
| MS-SWIFT | Alibaba's own SFT framework |
| verl | RL training (GRPO, PPO) |

## Dataset Formats

### SFT (Alpaca-style):
```json
{"instruction": "...", "input": "...", "output": "..."}
```

### SFT (ShareGPT-style):
```json
{
  "conversations": [
    {"from": "human", "value": "..."},
    {"from": "gpt", "value": "..."}
  ]
}
```

### GRPO / RL (with reward):
```json
{
  "prompt": "...",
  "chosen": "...",
  "rejected": "...",
  "reward": 0.9
}
```

## Key Performance Claims

- Qwen3-4B rivals Qwen2.5-72B-Instruct
- Qwen3-30B-A3B outperforms QwQ-32B (10× fewer activated params)
- Qwen3-235B-A22B competitive with DeepSeek-R1, o1, o3-mini, Gemini-2.5-Pro
