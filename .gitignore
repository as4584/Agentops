
# Secrets
.env
.env.*
!.env.example
*.key

# Runtime
.dashboard_port
.discord_bot.pid
.agent-sessions/

# Scratch / temp outputs
python_results.json
chatlog.md

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# Node / Next.js
node_modules/
.next/
out/
frontend/.next/
frontend/node_modules/

# OS
.DS_Store
Thumbs.db

# Windows Lighthouse CI temp profiles (created by LHCI on Windows, written into project dir)
**/lighthouse.*/
**/C:\\Users\\*

# Pytest
.pytest_cache/

# TypeScript build info
**/tsconfig.tsbuildinfo

# Agent context (local only, never push)
.agentctx/

# ML training data (cloned from GitHub, never push into parent repo)
.training_data/

# Embedded git repos (submodules or cloned reference projects)
SigmaSimulator/
sandbox/everything-claude-code/
sandbox/ui-ux-pro-max-skill/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Backups
*.bak
*.bak2

# Logs
backend/logs/*.jsonl

# Build outputs
output/

# Runtime agent data store (agent memory, scheduler.db, webhooks.json)
# These are created at runtime — do not track. Restore with: mkdir -p data/agents
data/*.db
data/*.json
data/agents/

# Browser profiles and user data (sensitive, large)
data/handshake_profile/
data/handshake_resumes/

# Social media runtime token cache (sensitive)
backend/memory/social_media/*.json

# Generated reports/artifacts
reports/lhci/mobile/
reports/lhci/desktop/
reports/playwright/playwright-report/
reports/playwright/test-results/

# Local sandbox runtime (ephemeral)
/tmp/ai-sandbox/

playground/sandbox

playground
# Higgsfield — session cookies (contain auth tokens, never commit)
data/higgsfield/.session_cookies.json
data/higgsfield/evidence/
*.coverage
.coverage
htmlcov/
backend/ml/experiments/
*.so
.openclaw/
.openclaw/

# Sensitive content - NEVER push
content_creation_pack/
sandbox/tmp/

# Generated live training / DPO / routing data (runtime churn, never commit)
data/training/live_*.jsonl
data/dpo/live_*.jsonl

# ML monitoring metrics (runtime-generated)
backend/ml/monitoring/metrics.jsonl

# GSD auto-generated architecture docs
docs/gsd/

# Soul core runtime state
data/agents/soul_core/
.gitnexus
